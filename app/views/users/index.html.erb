<h1>All users</h1>

<ul class="users">
	<% @users.each do |user| %>
		<li><%= link_to user.first_name, user %>
			<% if current_user.admin? %>
				<%= button_to "destroy", user, :method => :delete %>
			<% end %>
		</li>
	<% end %>
</ul>
